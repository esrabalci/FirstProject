/**

3. Create an Integer variable called startingNumber
Create a loop to countdown from starting number till 0. 

 */


//task3
 for(Integer startingNumber = 20 ; startingNumber >=0 ; startingNumber -=1 ){
        System.debug(startingNumber);
 }